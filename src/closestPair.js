export default function closestPair(point) {
  let mid = (point.length/2)
  let mid2 = (point.length/(2+1))
  let subArray = point.slice(0, mid)
  let subArray2 = point.slice(mid2, (point.length-1))


}
